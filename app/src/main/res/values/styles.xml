<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TranslucentTheme" parent="AppTheme">
        <!--在Android 4.4之前的版本上运行，直接跟随系统主题-->
    </style>

    <style name="ClubToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="contentInsetStart">0dp</item>
    </style>

    <declare-styleable name="RoundImageView">
        <attr name="border_thickness" format="dimension" />
        <attr name="border_inside_color" format="color" />
        <attr name="border_outside_color" format="color" />
        <attr name="borderRadius" format="dimension" />
        <attr name="type">
            <enum name="circle" value="0" />
            <enum name="round" value="1" />
        </attr>
    </declare-styleable>

    <!--自定义edittext的样式-->
    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/home_read</item>
        <item name="colorControlActivated">@color/home_read</item>
    </style>

    <style name="MyEditTextdown" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/textBlack</item>
        <item name="colorControlActivated">@color/textBlack</item>
    </style>

    <style name="MyEditTextlogin" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/fengexian</item>
        <item name="colorControlActivated">@color/fengexian</item>
    </style>

    <!--设置app秒开-->
    <!--<style name="AppTheme.FullScreen">-->
        <!--<item name="android:windowFullscreen">true</item>-->
        <!--<item name="android:windowBackground">@mipmap/welcom</item>-->
        <!--<item name="windowActionBar">false</item>-->
        <!--<item name="windowNoTitle">true</item>-->
    <!--</style>-->


</resources>
